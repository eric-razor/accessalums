<%= form_for @pitch,method: "put" do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, :value => @pitch.title %>

  <%= f.label :content %>
  <%= f.text_area :content, :alias => params[:@content] %>
<!--  I have no idea why the above code works-->
  <%= f.submit %>
<% end %>

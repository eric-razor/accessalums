<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
  </head>
  <link rel="stylesheet" href="application.scss">
  <body>
    <div id="container">
      <div id="header">
        <%= @pitch.title %></li>
      <%= @pitch.content %></li>
    <!-- <%= @pitch.student_id %></li> -->

    <ul>
      <% @comments.each do |comment|  %>
        <li>
          <%= comment.content %>
        </li>
      <% end %>
    </ul>

    <%= form_for @comment, url: pitch_comments_path(@pitch) do |f| %>
    <%= f.text_field :content, placeholder: "Write a comment..." %>
    <%= f.submit %>
    <br>

    <% if current_student == @pitch.student %>
      <%= link_to "Edit Pitch", edit_pitch_path%>
    <% end %>
  <% end %>
</div>
</div>
</body>
</html>
